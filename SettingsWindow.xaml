<Window x:Class="Paddy.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Paddy — Settings"
        Width="480"
        Height="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#FF1E1E1E"
        Foreground="#FFE0E0E0"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#FFD0D0D0"/>
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background"
                    Value="#FF2A2A2A"/>
            <Setter Property="Foreground"
                    Value="#FFF2F2F2"/>
            <Setter Property="BorderBrush"
                    Value="#FF555555"/>
            <Setter Property="Height"
                    Value="26"/>
            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground"
                    Value="#FFD0D0D0"/>
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background"
                    Value="#FF2A2A2A"/>
            <Setter Property="Foreground"
                    Value="#FFF2F2F2"/>
            <Setter Property="BorderBrush"
                    Value="#FF555555"/>
            <Setter Property="Height"
                    Value="26"/>
            <Setter Property="Padding"
                    Value="6,3"/>
            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>
            <Setter Property="CaretBrush"
                    Value="White"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background"
                    Value="#FF353535"/>
            <Setter Property="Foreground"
                    Value="#FFE0E0E0"/>
            <Setter Property="BorderBrush"
                    Value="#FF555555"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="Padding"
                    Value="12,4"/>
            <Setter Property="Height"
                    Value="28"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="#FF454545"/>
                            </Trigger>
                            <Trigger Property="IsPressed"
                                    Value="True">
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="#FF252525"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GroupHeader"
                TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#FF9E9E9E"/>
            <Setter Property="FontSize"
                    Value="10"/>
            <Setter Property="FontWeight"
                    Value="Bold"/>
            <Setter Property="Margin"
                    Value="0,0,0,6"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0"
                VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- ── Recording Format ── -->
                <TextBlock Text="RECORDING FORMAT"
                        Style="{StaticResource GroupHeader}"/>
                <Border Background="#FF252525"
                        BorderBrush="#FF383838"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12,10"
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                Grid.Column="0"
                                Text="Sample rate:"/>
                        <ComboBox x:Name="SampleRateCombo"
                                Grid.Row="0"
                                Grid.Column="1"/>

                        <TextBlock Grid.Row="2"
                                Grid.Column="0"
                                Text="Bit depth:"/>
                        <ComboBox x:Name="BitDepthCombo"
                                Grid.Row="2"
                                Grid.Column="1"/>

                        <TextBlock Grid.Row="4"
                                Grid.Column="0"
                                Text="Channels:"/>
                        <ComboBox x:Name="ChannelsCombo"
                                Grid.Row="4"
                                Grid.Column="1"/>
                    </Grid>
                </Border>

                <!-- ── Save Folder ── -->
                <TextBlock Text="SAVE FOLDER"
                        Style="{StaticResource GroupHeader}"/>
                <Border Background="#FF252525"
                        BorderBrush="#FF383838"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12,10"
                        Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="SaveFolderBox"
                                 Width="280"
                                 IsReadOnly="True"
                                 Margin="0,0,8,0"/>
                        <Button Content="📁 Browse…"
                                Click="BrowseFolder_Click"/>
                    </StackPanel>
                </Border>

                <!-- ── Buffer Recording ── -->
                <TextBlock Text="BUFFER RECORDING"
                        Style="{StaticResource GroupHeader}"/>
                <Border Background="#FF252525"
                        BorderBrush="#FF383838"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12,10"
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                Text="Past buffer duration:"/>
                        <Slider x:Name="BufferDurationSlider"
                                Grid.Column="1"
                                Minimum="3"
                                Maximum="60"
                                TickFrequency="1"
                                VerticalAlignment="Center"
                                ValueChanged="BufferDurationSlider_Changed"/>
                        <TextBlock x:Name="BufferDurationLabel"
                                   Grid.Column="2"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center"
                                   Text="10s"/>
                    </Grid>
                </Border>

                <!-- ── Global Hotkey ── -->
                <TextBlock Text="BUFFER TRIGGER HOTKEY"
                        Style="{StaticResource GroupHeader}"/>
                <Border Background="#FF252525"
                        BorderBrush="#FF383838"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12,10"
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                Grid.Column="0"
                                Text="Modifiers:"/>
                        <StackPanel Grid.Row="0"
                                Grid.Column="1"
                                Orientation="Horizontal">
                            <CheckBox x:Name="ModCtrl"
                                    Content="Ctrl"
                                    Margin="0,0,12,0"/>
                            <CheckBox x:Name="ModAlt"
                                    Content="Alt"
                                    Margin="0,0,12,0"/>
                            <CheckBox x:Name="ModShift"
                                    Content="Shift"/>
                        </StackPanel>

                        <TextBlock Grid.Row="2"
                                Grid.Column="0"
                                Text="Key (click to capture):"/>
                        <TextBox x:Name="HotkeyKeyBox"
                                 Grid.Row="2"
                                Grid.Column="1"
                                 IsReadOnly="True"
                                 Cursor="Hand"
                                 ToolTip="Click then press any key to assign"
                                 PreviewKeyDown="HotkeyKeyBox_PreviewKeyDown"
                                 GotFocus="HotkeyKeyBox_GotFocus"
                                 LostFocus="HotkeyKeyBox_LostFocus"/>
                    </Grid>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- OK / Cancel -->
        <StackPanel Grid.Row="1"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="OK"
                    Width="80"
                    Margin="0,0,8,0"
                    Click="OkButton_Click"
                    IsDefault="True"/>
            <Button Content="Cancel"
                    Width="80"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
